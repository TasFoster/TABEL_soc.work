# Зависимости приложения. Microsoft Excel БОЛЬШЕ НЕ требуется —
# файлы формируются на чистом Python.
xlrd>=2.0      # чтение входных .xls (реестры, отчёт 071) и шаблона табеля
xlwt>=1.3      # запись .xls (табель, приложение к табелю)
odfpy>=1.4     # запись .ods (реестр, проезд)
openpyxl>=3.1  # чтение/запись .xlsx (услуги-деньги: шаблон с формулами)

# Только для сборки одно-файлового .exe (build.ps1):
# pyinstaller
